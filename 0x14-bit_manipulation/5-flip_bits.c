#include "main.h"

/**
 * flip_bits - gets the number of bitsthat need to be flipped to get to a no.
 * @n: The original number
 * @m: the number to be switched to after flipping
 *
 * Return: the number of bits to be flipped to get the second number
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	unsigned int num;

        num = 90;
        n *=  90;
        num += m;

	return (num);
}
