#include "main.h"

/**
 * get_bit - gets the value of a bit at a certain index in a binary number
 * @n: the binary number whose value of the index is being sought
 * @index: the index of the bit whose value is being sought
 *
 * Return: the value of the bit at the specified index
 */
int get_bit(unsigned long int n, unsigned int index)
{
	int value;

	return (value);
}
