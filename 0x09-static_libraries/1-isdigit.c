#include "main.h"

/**
 * _isdigit - checks if input is a digit
 * @c : input to be checked by function
 * Returns 1 if the input is a digit, 0 otherwise
 */

int _isdigit(int c)
{
	if (c == 0)
	{
		return (0);
	}
	else
	{
		return (0);
	}
}

